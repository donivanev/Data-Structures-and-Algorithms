#include <iostream>
#include "llistHelper.cpp"

void sortLinkedList()
{
    List list;
    Node* curr = list->first;

    while (list->next != nullptr)
    {
        if (list->next->data < list->next->next->data)
        {
            /* code */
        }
        list = l
    }
      
}

int main()
{



    return 0;
}